#include "expression.h"
#include <stdio.h>

int main(int argc, char **argv)
{
	if (argc != 2)
	{
		printf("Usage : expression <mathematical expression to evaluate>\n");
		return (0);
	}
	return (0);
}
